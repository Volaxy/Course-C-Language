#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
    Acessar o diretório onde está o arquivo nomedoprograma.c
    Digitar o comando gcc -v para verificar se o gcc está instalado
    Digitar no prompt o comando: gcc nomedoprograma.c -o nomedoexecultavel.c
    Digitar o nomedoexecultavel.c
    */
    printf("Hello world!\n");
    return 0;
}
