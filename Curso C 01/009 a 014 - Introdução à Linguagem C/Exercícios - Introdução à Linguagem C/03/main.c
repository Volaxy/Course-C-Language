#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b, c;
    printf("Digite 3 numeros\n");
    scanf("%d %d %d", &a, &b, &c);
    printf("A soma dos numeros digitados e %d", a + b + c);

    return 0;
}
