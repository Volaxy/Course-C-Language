#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x, y, z;
    printf("Digite 3 valores\n");
    scanf("%d %d %d", &x, &y, &z);
    printf("A soma dos quadrados dos 3 valores lidor e %d", x * x + y * y + z * z);

    return 0;
}
